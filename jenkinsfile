properties([parameters([choice(choices: ['Patch','Minor','Major'], description: 'Third stage meassage', name: 'ImageTag')]), string(name: "application", defaultValue: "frontend", trim: false, description: "DESCRIPTION")])
pipeline{
  agent any
   environment {
    DOCKERHUB_CREDENTIALS=credentials('dockerhub')
  }
  stages{
        stage('Run Tests') {
          steps{
            script{
                   echo "hello"
                    }
                }
           }     
    }
}    
  
